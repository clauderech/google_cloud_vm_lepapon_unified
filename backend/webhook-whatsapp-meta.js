/**
 * Compatibilidade: este módulo agora apenas re-exporta as rotas.
 * As rotas oficiais estão em `routes/webhook-whatsapp-meta.js`.
 */

'use strict';

export { default } from "./routes/webhook-whatsapp-meta";
